import { SafePipe } from './safe.pipe';
import { DomSanitizer } from '@angular/platform-browser';

describe('Shared -> Pipes -> Safe Pipe', () => {
	/*
	let sanetizer: DomSanitizer;
  let pipe = new SafePipe(sanetizer);

  it('should return null data', () => {
    expect(pipe.transform('url')).toBe('url');
  });
*/
});
